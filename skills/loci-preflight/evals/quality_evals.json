{
  "skill_name": "loci-preflight",
  "description": "Tests that the content of each preflight section is correct and specific per SKILL.md — not just present. Call graph must reference actual CFG findings. Energy must report values from the LOCI MCP. Execution fit must follow from the findings. Uses functions compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
  "note": "Set $LOCI_TEST_BLE_ROOT to your local BLE project root before running.",
  "evals": [
    {
      "id": "pf-quality-1",
      "should_trigger": true,
      "category": "call-graph-quality-indirect-call",
      "prompt": "Add a logging hook to osal_set_event in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/osal/src/common/osal.c that calls a user-supplied function pointer logFn(taskID, event) right before the event is posted. logFn is stored in a global and set by the application at startup.",
      "expected_output": "A preflight call graph section that flags the existing indirect calls (blx r0, blx r1) already in osal_set_event's CFG as CFI hazards, and identifies that adding another indirect call deepens that risk. The skill should note the indirect dispatch pattern is already present in the assembly, not guess about it generically.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a preflight report section",
          "description": "Adding an indirect-call callee requires preflight"
        },
        {
          "text": "Preflight call graph section flags an indirect call or CFI hazard - either from the existing blx-to-register pattern in osal_set_event's CFG or from the new function pointer callee being added",
          "description": "SKILL.md: 'any bl to a register in a callee's CFG - flag as a potential CFI hazard'. osal_set_event has blx r0 and blx r1 in its compiled CFG."
        }
      ]
    },
    {
      "id": "pf-quality-2",
      "should_trigger": true,
      "category": "call-graph-quality-call-order",
      "prompt": "Add a call to GAPBondMgr_FindAddr as the first thing inside GAPBondMgr_ProcessEvent in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/host/gapbondmgr.c to verify that the event's source address is bonded before processing the event. This will be added before any other logic in the function.",
      "expected_output": "A preflight call graph section that checks whether GAPBondMgr_FindAddr's CFG shows a guard or assertion that GAPBondMgr_Init has already run. If no such guard is visible in the compiled CFG, the skill must flag the call-order assumption as a risk.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a preflight report section",
          "description": "Adding a callee with an init dependency requires preflight"
        },
        {
          "text": "Preflight call graph section checks whether GAPBondMgr_FindAddr enforces or assumes that GAPBondMgr_Init has already run - it must flag the absence of an init guard in the callee's CFG as a call-order risk",
          "description": "SKILL.md: 'if the new function must be called after an init(), check whether the callee's CFG shows any guard or assertion enforcing that order. If not, flag it.'"
        }
      ]
    },
    {
      "id": "pf-quality-3",
      "should_trigger": true,
      "category": "call-graph-quality-latency",
      "prompt": "Add a call to GAPBondMgr_FindAddr at the top of HCI_SendDataPkt in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/hci/cc26xx/hci.c to reject packets from unbonded peers. The HCI data path must complete within 10μs at 48 MHz (480 cycles). Both functions are in $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "A preflight call graph section that runs asm-analyze on GAPBondMgr_FindAddr and identifies the characteristics that make it too slow for the 480-cycle budget - specifically referencing the NV read path or the RPA resolution loop visible in the CFG. The section must not just say 'this might be slow' but cite specific CFG findings.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a preflight report section",
          "description": "Adding a slow callee on a timing-constrained hot path requires preflight"
        },
        {
          "text": "Preflight call graph section flags a latency or timing risk for GAPBondMgr_FindAddr and references specific characteristics (NV access, RPA resolution loop, or cycle count estimate) rather than a generic 'it might be slow'",
          "description": "SKILL.md: 'check response-time data for callees on the hot path and flag any that would violate a timing budget' - the explanation must be specific to this callee's CFG"
        },
        {
          "text": "Preflight Execution fit is ADJUST PLAN or STOP",
          "description": "NV-reading callee on a sub-10μs path cannot be GOOD"
        }
      ]
    },
    {
      "id": "pf-quality-4",
      "should_trigger": true,
      "category": "execution-fit-quality-derives-from-findings",
      "prompt": "Add a simple boolean guard inside HCI_SendDataPkt in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/hci/cc26xx/hci.c that returns HCI_ERROR_CODE_CONTROLLER_BUSY if a global flag hciSuspended is set - no new callees, no allocation. HCI_SendDataPkt is compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "A preflight report where all checks are OK because there are no new callees (call graph OK), no timing concern (latency OK), and negligible energy (energy OK). Execution fit must be GOOD - the skill must not invent risks that are not present.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a preflight report section",
          "description": "Even a simple change requires preflight - the skill should run and confirm it is clean"
        },
        {
          "text": "Preflight Execution fit is GOOD",
          "description": "No callees, no timing risk, no energy risk - all checks are OK so fit must be GOOD, not ADJUST PLAN"
        },
        {
          "text": "Preflight call graph, latency, and energy lines all show OK (not a risk or block)",
          "description": "SKILL.md: 'Keep it short when things are clean' - the skill must not hallucinate risks when none exist"
        }
      ]
    }
  ]
}
